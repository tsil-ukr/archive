name: Static Deploy

on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Build and push to static repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 21.x
        uses: actions/setup-node@v4
        with:
          node-version: "21.x"
      - run: |
          npm i -g xdocs
          git clone https://github.com/tsil-ukr/archive-static .будування
          git clone https://github.com/tsil-ukr/website .вебсайт
          bash згенерувати.sh
          cd .будування
          git add .
          git commit -m "🧬"
          git push